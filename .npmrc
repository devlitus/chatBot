# Handle optional dependencies properly
optional=false

# Ensure proper platform-specific binaries
platform=linux
arch=x64

# Disable optional dependencies that cause issues in CI
rollup-optional-dependencies=false 